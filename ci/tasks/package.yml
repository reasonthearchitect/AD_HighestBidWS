platform: linux

image_resource:
  type: docker-image
  source: {repository: niaquinto/gradle}

inputs:
  - name: resource-highestbid

outputs:
  - name: resource-jar

run:
  path: ./resource-highestbid/ci/scripts/package.sh

